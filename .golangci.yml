version: "2"

run:
  timeout: 5m
  issues-exit-code: 2
  tests: false
  modules-download-mode: readonly
  go: "1.25"

linters:
  default: none
  enable:
    - asasalint

issues:
  exclude-generated: strict
